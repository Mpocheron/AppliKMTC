{% extends 'base.html.twig' %}

{% block title %}Récapitulatif de la commande{% endblock %}

{% block content %}
<p class="marge-navbar"> </p>
<div class="page-recap">
<p class="commande-reussie">Commande réussie !</p>
<br>
<h1>Récapitulatif de la commande</h1>

<p>Commande numéro : {{ commande.id }}</p>

<h2>Expéditeur :</h2>
<p>
    {{ commande.adresseExpedition.numero }} {{ commande.adresseExpedition.nom }}<br>
    {{ commande.adresseExpedition.codePostal }} {{ commande.adresseExpedition.ville }}
</p>

{# <h2>Destinataire :</h2>
    {% if commande.adresseDestination is not null %}
        <p>{{ commande.adresseDestination.numero }} {{ commande.adresseDestination.nom }}<br>
        {{ commande.adresseDestination.rue }}<br>
        {{ commande.adresseDestination.codePostal }} {{ commande.adresseDestination.ville }}</p>
    {% else %}
        <p>Aucune adresse de destination spécifiée.</p> 
    {% endif %} #}
<h2>Destinataire :</h2>
<p>
    Nom : {{ commande.nomDestinataire }}<br>
    Prénom : {{ commande.prenomDestinataire }}
</p>
    <h2>Destination :</h2>
    {% if commande.adresseDestination is not null %}
        <p>{{ commande.adresseDestination.numero }} {{ commande.adresseDestination.nom }}<br>
        {{ commande.adresseDestination.codePostal }} {{ commande.adresseDestination.ville }}</p>
    {% else %}
        {% if commande.relais is not null %}
            <p>ID Relais : {{ commande.relais.id }}</p>
            <p>Adresse du relais : {{ commande.relais.adresseComplete }}</p>
        {% else %}
            <p>Aucune destination spécifiée.</p>
        {% endif %}
    {% endif %}

{# {% if commande.adresseDestination is not null %}
        <h2>Destinataire :</h2>
        <p>{{ commande.adresseDestination.numero }} {{ commande.adresseDestination.nom }}<br>
           {{ commande.adresseDestination.rue }}<br>
           {{ commande.adresseDestination.codePostal }} {{ commande.adresseDestination.ville }}</p>
    {% else %}
        {% if commande.relais is not null %}
            <h2>Relais :</h2>
            <p>{{ commande.relais.nom }}<br>
               {{ commande.relais.adresseComplete }}</p>
        {% else %}
            <p>Aucune destination spécifiée</p>
        {% endif %}
    {% endif %} #}
<h2>Adresse de facturation :</h2>
<p>
    {{ commande.adresseFacturation.numero }} {{ commande.adresseFacturation.nom }}<br>
    {{ commande.adresseFacturation.codePostal }} {{ commande.adresseFacturation.ville }}
</p>

<h2>Détails de la commande :</h2>
<p>Hauteur : {{ commande.hauteur }} cm</p>
<p>Largeur : {{ commande.largeur }} cm</p>
<p>Longueur : {{ commande.longueur }} cm</p>
<p>Poids : {{ commande.poids }} kg</p>



{# <a href="{{ path('app_commande') }}">Retour à la page de commande</a> #}
</div>
{% endblock %}
